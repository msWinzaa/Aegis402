fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pytest>=8.0
httpx>=0.27

# Optional: only required to use X402FacilitatorAdapter (real x402 SDK
# integration) instead of MockX402Adapter. Not needed for the demo, the
# test suite, or MockX402Adapter-based development.
# x402[httpx]>=2.18

# Configuration

See `.env.example` for public configuration. Important values include node ID/key, local DB/ledger paths, x402 adapter/facilitator/version/network/asset, peer bootstrap token, peer input/rate limits, clock skew and peer timeout.

`AEGIS_NODE_SIGNING_KEY` is an Aegis node identity secret. It is never a consumer wallet key.
